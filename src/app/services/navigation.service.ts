import {Injectable} from '@angular/core';
import { environment } from 'src/environments/environment';

@Injectable()
export class NavigationService {
  // navigateToBackend(path: string) {
  //   window.location.href = environment.backend.url + '/' + path;
  // }
}
